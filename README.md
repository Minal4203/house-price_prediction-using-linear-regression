# 🏡 House Price Prediction using Linear Regression

## 📌 Overview
This project demonstrates how to predict house prices using **Linear Regression**, a fundamental machine learning algorithm.  
We use a dataset containing features such as the number of rooms, area, location, and other attributes to train a regression model that estimates housing prices.

---

## ⚙️ Requirements
- Python 3.8+
- Libraries:
  - pandas
  - numpy
  - scikit-learn
  - matplotlib
  - seaborn
  - jupyter (optional for notebooks)

Install dependencies:
```bash
pip install -r requirements.txt
